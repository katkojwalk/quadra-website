pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Quadra website files checked successfully'
                sh 'ls -la'
            }
        }
    }
}
